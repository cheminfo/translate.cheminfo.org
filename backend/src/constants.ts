/** Default listening port, derived from the project creation date (2026-09-14). */
export const DEFAULT_PORT = 10_914;

/** How many contributions one client address may submit per hour by default. */
export const DEFAULT_CONTRIBUTIONS_PER_HOUR = 20;

/** The repositories pull requests may be opened on when nothing says otherwise. */
export const DEFAULT_ALLOWED_REPOSITORIES = 'cheminfo/*';
